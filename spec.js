const mocha = require('mocha');
const expect = require('chai').expect;
const game = require('./game.js');

describe('Game', function() {
  //i need a display board function to show the game state between plays
  // describe('display board', function() {
  //   it('should display the current state of the gameboard', function(done) {
     
  //   });
  // });
  //i need a 'is valid move' checking function to make sure the player can make a move
  //i need a 'is game over 'function to see if there is a winner or a draw
  //i need a 'turn tracker' function to determine if it's X or 0's turn
});